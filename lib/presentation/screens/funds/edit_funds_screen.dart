import 'package:flutter/material.dart';

class EditFundsScreen extends StatelessWidget {
  const EditFundsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Edit Funds (Stub)'),
      ),
    );
  }
}
